
language: swift

matrix:
    include:
        - os: osx
          osx_image: xcode8
        # - os: linux
        #   dist: trusty
        #   sudo: required

script:
    - swift package build
    - swift package test
